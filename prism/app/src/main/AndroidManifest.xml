<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Storage access for local video files + 3D models -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Full file access for non-media files (.glb, .gltf, .funscript, .hsp) -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <!-- Network access for streaming content -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- XR permissions -->
    <uses-permission android:name="android.permission.SCENE_UNDERSTANDING_COARSE" />
    <uses-permission android:name="android.permission.SCENE_UNDERSTANDING_FINE" />
    <uses-permission android:name="android.permission.EYE_TRACKING_COARSE" />
    <uses-permission android:name="android.permission.EYE_TRACKING_FINE" />
    <uses-permission android:name="android.permission.FACE_TRACKING" />

    <!-- Hardware sensor permissions -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.HIGH_SAMPLING_RATE_SENSORS" />

    <!-- Audio capture for beat-reactive lighting (Visualizer API) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Bluetooth for Lovense haptic device control -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- XR features. The required=true Android XR features (api.spatial, api.openxr,
         input.controller) live in the galaxyxr flavor manifest — they would make the
         Horizon OS installer reject the APK on Quest. -->
    <uses-feature android:name="android.hardware.xr.input.eye_tracking" android:required="false" />
    <uses-feature android:name="android.hardware.xr.input.face_tracking" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.compass" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.barometer" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.stepcounter" android:required="false" />

    <!-- TODO: Replace with custom ChloeVR icon (512x512 PNG) before Play Store submission -->
    <application
        android:label="ChloeVR"
        android:icon="@android:drawable/ic_media_play"
        android:theme="@android:style/Theme.DeviceDefault.NoActionBar"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- ChloeVR expects room-scale movement in Full Space; request Android XR's larger safety boundary. -->
        <property
            android:name="android.window.PROPERTY_XR_BOUNDARY_TYPE_RECOMMENDED"
            android:value="XR_BOUNDARY_TYPE_LARGE" />

        <!-- OpenXR native runtime -->
        <uses-native-library android:name="libopenxr.google.so" android:required="false" />

        <!-- MainActivity (Jetpack XR / SceneCore video player) is declared in the
             galaxyxr flavor manifest. On Quest the launcher is FilamentModelActivity,
             promoted via the quest flavor manifest. -->

        <!-- Filament + OpenXR direct renderer for 3D models (UNMANAGED mode).
             NO separate taskAffinity: a 2026-06 Galaxy XR OTA added a
             BoundarySetupActivity interceptor to every unmanaged-XR launch;
             when its empty task finishes, focus returns to the LAUNCHING task —
             with the viewer in its own task that demoted it to back (session
             killed ~0.5s after FOCUSED, user bounced to desktop). Sharing
             MainActivity's task makes the viewer the top of the focus-return
             target instead. (Affinity dated from c59e6e2, a shotgun focus fix
             whose lifecycle/session-state handling was the real cure.) -->
        <!-- exported=true: lets adb (and shortcuts) launch the viewer directly,
             bypassing MainActivity's flaky Jetpack-XR panel startup race
             (Node.setIsRenderableAndAttached NPE on ~half of launches). -->
        <activity
            android:name=".FilamentModelActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="landscape">

            <property
                android:name="android.window.PROPERTY_XR_ACTIVITY_START_MODE"
                android:value="XR_ACTIVITY_START_MODE_FULL_SPACE_UNMANAGED" />
        </activity>

        <!-- 2D trampoline that collects runtime permissions BEFORE the OpenXR
             viewer starts. The viewer must never call requestPermissions —
             the dialog backgrounds the immersive activity and kills the XR
             session — so launch paths that skip MainActivity (Quest launcher,
             adb direct launch) bounce through this gate once. -->
        <activity
            android:name=".PermissionGateActivity"
            android:exported="false" />
    </application>
</manifest>
